/* const accountOne = 000256784;
const accountTwo = 000986056;
const accountThree = 001546238;
const accountFour = 030987456;

const balanceOne = 790;
const balanceTwo = 5600;
const balanceThree = 78065;
const balanceFour = 120910;

const enteredAccountNumber = prompt('Please enter your account number');

if (enteredAccountNumber = accountOne) {
    document.write(balanceOne);
} else if (enteredAccountNumber = accountTwo) {
        document.write(balanceTwo);
    } else if (enteredAccountNumber = accountThree) {
        document.write(balanceThree);
    } else if (enteredAccountNumber = accountFour) {
        document.write(balanceFour)
    } else {
        document.write('Your account number is invalid.')
    }
 */

 /* ENTER PASSWORD AND USERNAME */

 /* const username = prompt("Please enter your username");
 const password = prompt("Now please enter your password");

 if (password === 'sore-thumb-2018') {
     document.write('Access granted!');
 } else {
     document.write('Nice try.');
 } */

/* PASSWORD VERIFICATION CHECK */

/*  const password = prompt("Please choose a password");
 const verifyPassword = prompt('Please re-enter your password');

 if (verifyPassword.length < 8) {
    document.write('Your password is not secure.');
} else if (password === verifyPassword) {
     document.write('Password verification confirmed');
 } else {
     document.write('Your passwords do not match');
 } */

 /* BUS TICKET PRICES */
 
/* const age = prompt('Please enter your age');
const ticketPrice = 12;

if (age < 6) {
    alert('Your ticket price is 0 euro')
} else if (age < 15) {
    alert('Your ticket price with pupil discount is: ' + Math.floor(ticketPrice * 0.35) + ' euros.');
} else if (age < 26) {
    alert('Your ticket price with student discount is: ' + Math.floor(ticketPrice * 0.65) + ' euros.');
} else if (age < 64) {
    alert('Your ticket price is: ' + ticketPrice + ' euros.');
} else if (age >= 64) {
    alert('Your ticket price with seniors discount is: ' + Math.floor(ticketPrice * 0.5) + ' euros.');
};
 */
/* 
 // Mathematical Functions
 
 const square = (a) => {
     return a * a; 
 };

 const hypotenuse = (a, b) => {
     return Math.sqrt(square(a) + square(b));
 }

 // Salary Function

 const salary = (pay, hours, days) => {
     return pay * hours * days; 
 }

 const taxedSalary = (sal, tax) => {
     return sal * (1 - tax);
 } */
